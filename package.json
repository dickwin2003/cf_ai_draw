{
  "name": "cf-ai-draw",
  "version": "1.0.0",
  "description": "AI Drawing Application based on Cloudflare Workers",
  "main": "worker.js",
  "scripts": {
    "build": "npm install && echo \"No build step required\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
